#include <iostream>
#include<cmath>
int main(){
    double x=3;
    double y=4;
    double z;
    // z=std::max(x,y);
    // z=std::min(x,y);
    // z=pow(2,4);
//   z=sqrt(64);
    // z=abs(-3);
    // z=round(2.6);
    // z=ceil(2.1);
    z=floor(2.9);
    std::cout<<z;
    
    return 0;
}